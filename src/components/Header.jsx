export default function Header(){

    const style = {
        backgroundColor: "blue",
        color: "green",
    };

    return (
        <header style={style}>
            <h2>Feedback UI</h2>
        </header>
    );
}